# MonoList v0.4.2 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 恢复可靠菜单栏定位并完成交互、布局、更新和构建管理修复。

**Architecture:** 使用原生状态栏文字和确定性屏幕锚点；主窗口继续由 `WindowCoordinator` 统一管理。构建脚本沿用 ShotLens 的 local/release 分层，发布后只清理临时目录。

**Tech Stack:** Swift、SwiftUI、AppKit、Shell Smoke Tests。

---

### 任务一：状态栏与窗口定位

- [ ] 增加原生状态栏文字及回退锚点失败测试。
- [ ] 删除附件徽标，改为稳定的 attributed text。
- [ ] 增加主屏幕顶部回退锚点。
- [ ] 移除菜单栏和窗口之间的额外间距。
- [ ] 为清单关闭增加上收淡出动画。

### 任务二：待办交互与紧凑布局

- [ ] 增加编辑 Enter 后新增和空白双击新增测试。
- [ ] 恢复编辑 Enter 的新增回调，点击空白仍只保存。
- [ ] 双击空白时在末尾展示输入行。
- [ ] 缩短列表底部和轻提醒上下留白。

### 任务三：设置与更新

- [ ] 让整个设置选择控件包括箭头区域都由同一个 Menu 接收点击。
- [ ] 对齐 ShotLens 更新状态文字和颜色反馈。
- [ ] 验证手动检测与升级按钮。

### 任务四：构建、验证与发布

- [ ] 增加项目完整性与构建清理脚本。
- [ ] 保留 `build/local/MonoList.app` 和最新 DMG。
- [ ] 自动删除测试目录、DMG staging、`.DS_Store` 和临时预览。
- [ ] 运行全部测试、签名和 DMG 校验。
- [ ] 发布 v0.4.2 并验证应用内检测。
