# MonoList v0.7.4

本次更新修复拖动结束后蓝色插入线偶尔残留的问题：

- 松开鼠标时立即清除插入线，不再等待异步读取和保存完成。
- 插入线不再参与鼠标命中，确保 drop 事件交给真实待办行处理。
- 拖动数据解析或保存失败时也会完整结束拖动状态。
- 每次拖动使用独立会话 ID，较晚返回的旧回调不会清除下一次拖动状态。
- 拒绝没有 MonoList 内部拖动会话的外部文本 drop。
