# MonoList v0.7.2

本次更新修复待办拖动光标、跟随样式和落点识别：

- 拖动待办时统一使用移动操作，不再显示复制操作的“+”光标。
- 使用完整待办行作为拖动预览，让内容稳定跟随鼠标。
- 原列表保持稳定，目标位置使用轻量插入线反馈，避免动画移动命中区域造成落点跳动。
- 根据每条待办的真实显示高度判断鼠标位于上半区或下半区，长文本也能正确识别插入位置。
- 空分组、组尾和正在输入的草稿行都可以连续接收拖放，不再形成光标或落点断层。
- 松开鼠标后才保存排序，取消或移出目标不会修改任务数据。
