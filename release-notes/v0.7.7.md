## MonoList v0.7.7

本版本修复 MonoList 已在后台运行、但菜单栏入口仍不显示的问题。

- 菜单栏入口改由 MonoList 主程序直接创建，不再依赖单独的辅助进程。
- 恢复菜单栏 Logo、短期待办数量、左键打开列表和右键菜单。
- MonoList 继续作为纯菜单栏 App 运行，不会额外显示 Dock 图标。

普通升级不会清除待办、提醒设置或其他用户数据。

> 本版本使用本地自签名，不代表 Apple 认证。首次启动可能出现 Gatekeeper 提示。
