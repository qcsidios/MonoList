# MonoList v0.7.5

本次更新进一步修复拖动完成后蓝色插入线残留的问题：

- 松开鼠标时原子取出落点并立即关闭整个拖动会话，蓝线同步消失。
- 会话关闭后，系统迟到的 `dropUpdated` 事件不能重新恢复插入线。
- 每个拖放代理都绑定创建时的会话 ID，旧拖动的更新或退出事件不会污染下一次拖动。
- 异步读取拖动数据只使用已保存的落点快照，不再修改界面拖动状态。
- 增加旧事件与新拖动交错顺序的回归测试。
